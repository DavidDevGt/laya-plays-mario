# Core ML / model
transformers==5.17.0
safetensors==0.8.0
tokenizers==0.23.2
huggingface_hub==1.32.0
numpy==2.5.3

# Mario emulator
gym_super_mario_bros==9.1.0
nes_py==9.0.1
gymnasium==1.3.0
pyglet==1.5.21

# torch is installed separately (CUDA build) -- see README.md:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# CPU-only machines: pip install torch
